import {title,shell,head,api,table,esc,formatDate,formatMs,codeBadge,state,usageText,badge} from './common.js'
export async function renderKeyDetail(){title('Key 详情');shell('key-detail',head('Key 详情','查看单个 Key 的关联账号、模型与请求日志')+'<div class="card"><span class="loader"></span></div>');const hash=location.hash.split('?')[1]||'';const query=new URLSearchParams(hash);const keys=await api('/keys');const explicit=query.get('id')||'';const id=explicit||state.detail.keyId||(keys[0]?.id)||'';state.detail.keyId=id;if(!id){shell('key-detail',`${head('Key 详情','查看单个 Key 的关联账号、模型与请求日志')}<section class="card"><div class="empty">暂无可用的 API Key</div></section>`);return}const [key,res]=await Promise.all([api(`/keys/${id}`),api(`/logs?page=${state.detail.page}&limit=50&api_key_id=${encodeURIComponent(id)}`)]);let usage=null,models=[];if(key.account?.id){try{usage=await api(`/accounts/${key.account.id}/usage`)}catch{}try{const d=await api(`/accounts/${key.account.id}/models`);models=Array.isArray(d.data)?d.data:[]}catch{}}const opts=keys.map(v=>`<option value="${esc(v.id)}" ${v.id===id?'selected':''}>${esc(v.name)}</option>`).join('');const rows=(res.logs||[]).map(v=>`<tr><td>${esc(formatDate(v.created_at))}</td><td><span class="mono">${esc(v.method||'-')}</span></td><td>${esc(v.model||'-')}</td><td>${v.prompt_tokens==null&&v.completion_tokens==null?'-':esc(`in ${v.prompt_tokens||0} / out ${v.completion_tokens||0}${v.total_tokens!=null?` / total ${v.total_tokens}`:''}`)}</td><td><span class="mono small">${esc(v.path||'-')}</span></td><td>${codeBadge(v.status_code)}</td><td>${esc(formatMs(v.duration_ms))}</td><td>${esc(v.error||'-')}</td></tr>`);const pages=Math.max(1,Math.ceil((res.total||0)/50));shell('key-detail',`${head('Key 详情','查看单个 Key 的关联账号、模型与请求日志',`<select id="detail-select" class="select" style="width:220px;">${opts}</select>${query.get('id')&&state.user.role==='admin'?'<button id="detail-back" class="btn">返回</button>':''}`)}<section class="card detail-hero"><div class="detail-hero-main"><div class="detail-title-row"><div><div class="muted small">当前选中 Key</div><h2 style="margin:6px 0 8px;">${esc(key.name)}</h2></div>${key.enabled?'<span class="badge success">启用中</span>':'<span class="badge danger">已禁用</span>'}</div><div class="detail-pill-row"><span class="detail-pill">请求数：${esc(key.request_count??0)}</span><span class="detail-pill">最后使用：${esc(formatDate(key.last_used_at))}</span><span class="detail-pill mono">${esc(key.masked_key||key.key||'-')}</span></div></div><div class="detail-hero-side"><div class="muted small">归属</div><div style="margin-top:8px;">${esc(key.owner_username||key.owner_id||state.user?.username||'-')}</div>${key.account?.name?`<div class="muted small" style="margin-top:12px;">关联账号</div><div style="margin-top:8px;">${esc(key.account.name)}</div>`:''}</div></section><section class="grid-3"><div class="card"><div class="card-title"><h2>Key 信息</h2></div><div class="kv-list"><div class="kv-row"><div class="key">名称</div><div>${esc(key.name)}</div></div><div class="kv-row"><div class="key">状态</div><div>${key.enabled?badge('enabled'):badge('disabled')}</div></div><div class="kv-row"><div class="key">Key 值</div><div class="mono small">${esc(key.masked_key||key.key||'-')}</div></div><div class="kv-row"><div class="key">请求数</div><div>${esc(key.request_count??0)}</div></div><div class="kv-row"><div class="key">最后使用</div><div>${esc(formatDate(key.last_used_at))}</div></div></div></div><div class="card"><div class="card-title"><h2>关联账号</h2></div>${key.account?`<div class="kv-list"><div class="kv-row"><div class="key">名称</div><div>${esc(key.account.name)}</div></div><div class="kv-row"><div class="key">类型</div><div>${esc(key.account.account_type)}</div></div><div class="kv-row"><div class="key">API 地址</div><div class="mono small">${esc(key.account.api_url)}</div></div><div class="kv-row"><div class="key">配额</div><div>${usageText(usage)}</div></div></div>`:'<div class="empty">未关联账号</div>'}</div><div class="card"><div class="card-title"><h2>可用模型</h2></div>${models.length?`<div class="details-box">${models.map(v=>`<div class="mono small detail-model-item">${esc(v.id)}${v.display_name?` <span class="muted">(${esc(v.display_name)})</span>`:''}</div>`).join('')}</div>`:'<div class="empty">暂无模型信息</div>'}</div></section><section class="card"><div class="card-title"><h2>请求日志</h2><button id="detail-refresh" class="btn small">刷新</button></div>${table(['时间','方法','模型','Tokens','路径','状态','耗时','错误'],rows,'暂无日志')}<div class="pagination"><button id="detail-prev" class="btn small" ${state.detail.page<=1?'disabled':''}>上一页</button><span class="muted small">第 ${esc(state.detail.page)} / ${esc(pages)} 页（共 ${esc(res.total||0)} 条）</span><button id="detail-next" class="btn small" ${state.detail.page>=pages?'disabled':''}>下一页</button></div></section>`);document.getElementById('detail-select').onchange=e=>{state.detail.page=1;state.detail.keyId=e.target.value;location.hash=`#/key-detail?id=${encodeURIComponent(e.target.value)}`};if(document.getElementById('detail-back'))document.getElementById('detail-back').onclick=()=>history.back();document.getElementById('detail-refresh').onclick=()=>renderKeyDetail();document.getElementById('detail-prev').onclick=()=>{if(state.detail.page>1){state.detail.page-=1;renderKeyDetail()}};document.getElementById('detail-next').onclick=()=>{if(state.detail.page<pages){state.detail.page+=1;renderKeyDetail()}}}
