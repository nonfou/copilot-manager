import {title,shell,head,api,table,esc,formatDate,toast,state,badge} from './common.js'
export async function renderKeys(){title('Key 管理');shell('keys',head('Key 管理','管理代理 Key 与归属账号')+'<div class="card"><span class="loader"></span></div>');const [accounts,keys,usersRes]=await Promise.all([api('/accounts'),api(`/keys${state.keys.filterAccountId?`?account_id=${encodeURIComponent(state.keys.filterAccountId)}`:''}`),api('/users').catch(()=>({users:[]}))]);const edit=keys.find(v=>v.id===state.keys.editId);const amap=new Map(accounts.map(v=>[v.id,v]));const aopts=accounts.map(v=>`<option value="${esc(v.id)}">${esc(v.name)}</option>`).join('');const fopts=['<option value="">所有账号</option>'].concat(accounts.map(v=>`<option value="${esc(v.id)}">${esc(v.name)}</option>`)).join('');const uopts=(usersRes.users||[]).map(v=>`<option value="${esc(v.id)}">${esc(v.username)}${v.role==='admin'?' (admin)':''}</option>`).join('');const rows=keys.map(k=>`<tr><td><strong>${esc(k.name)}</strong></td><td>${esc(k.owner_username||k.owner_id||'-')}</td><td>${esc(amap.get(k.account_id)?.name||k.account_id)}</td><td><span class="mono small">${esc(k.masked_key||k.key||'-')}</span></td><td>${badge(k.enabled?'enabled':'disabled')}</td><td>${esc(k.request_count??0)}</td><td>${esc(formatDate(k.last_used_at))}</td><td><div class="actions"><button class="btn small" data-act="detail" data-id="${esc(k.id)}">详情</button><button class="btn small" data-act="edit" data-id="${esc(k.id)}">编辑</button><button class="btn small warning" data-act="regen" data-id="${esc(k.id)}">重置</button><button class="btn small danger" data-act="delete" data-id="${esc(k.id)}">删除</button></div></td></tr>`);shell('keys',`${head('Key 管理','管理代理 Key 与归属账号')}${state.keys.revealedKey?`<section class="card"><div class="card-title"><h2>新生成的 Key</h2><button id="hide-key" class="btn small">隐藏</button></div><div class="details-box"><div class="mono">${esc(state.keys.revealedKey)}</div><div class="form-actions"><button id="copy-key" class="btn small">复制到剪贴板</button></div></div></section>`:''}<section class="card"><div class="card-title"><h2>创建 Key</h2></div><form id="key-create-form"><div class="form-grid"><div><label class="label">Key 名称</label><input class="input" name="name" /></div><div><label class="label">关联账号</label><select class="select" name="account_id">${aopts}</select></div><div><label class="label">归属用户（可选）</label><select class="select" name="owner_id"><option value="">默认跟随账号拥有者</option>${uopts}</select></div></div><div class="form-actions"><button class="btn primary">创建 Key</button></div></form></section>${edit?`<section class="card"><div class="card-title"><h2>编辑 Key</h2></div><form id="key-edit-form"><input type="hidden" name="id" value="${esc(edit.id)}" /><div class="form-grid"><div><label class="label">Key 名称</label><input class="input" name="name" value="${esc(edit.name)}" /></div><div><label class="label">状态</label><select class="select" name="enabled"><option value="true" ${edit.enabled?'selected':''}>启用</option><option value="false" ${!edit.enabled?'selected':''}>禁用</option></select></div></div><div class="form-actions"><button id="key-edit-cancel" type="button" class="btn">取消</button><button class="btn primary">保存修改</button></div></form></section>`:''}<section class="card"><div class="card-title"><h2>Key 列表</h2><div class="row wrap"><select id="key-filter" class="select" style="width:220px;">${fopts}</select><button id="keys-refresh" class="btn small">刷新</button></div></div>${table(['名称','归属用户','关联账号','Key 值','状态','请求数','最后使用','操作'],rows,'暂无 Key')}</section>`);document.getElementById('key-filter').value=state.keys.filterAccountId;document.getElementById('key-filter').onchange=e=>{state.keys.filterAccountId=e.target.value;renderKeys()};document.getElementById('keys-refresh').onclick=()=>renderKeys();if(document.getElementById('hide-key')){document.getElementById('hide-key').onclick=()=>{state.keys.revealedKey='';renderKeys()};document.getElementById('copy-key').onclick=async()=>{try{await navigator.clipboard.writeText(state.keys.revealedKey);toast('已复制到剪贴板','success')}catch{toast('复制失败','warning')}}}document.getElementById('key-create-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),p={name:String(f.get('name')||'').trim(),account_id:String(f.get('account_id')||'')};const oid=String(f.get('owner_id')||'').trim();if(!p.name||!p.account_id){toast('请填写 Key 名称并选择关联账号','warning');return}if(oid)p.owner_id=oid;try{const d=await api('/keys',{method:'POST',body:JSON.stringify(p)});state.keys.revealedKey=d.key||'';toast('Key 已创建','success');await renderKeys()}catch(error){toast(error.message,'error')}};if(document.getElementById('key-edit-form')){document.getElementById('key-edit-cancel').onclick=()=>{state.keys.editId='';renderKeys()};document.getElementById('key-edit-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),id=String(f.get('id')||'');const p={name:String(f.get('name')||'').trim(),enabled:String(f.get('enabled')||'true')==='true'};if(!p.name){toast('请填写 Key 名称','warning');return}try{await api(`/keys/${id}`,{method:'PUT',body:JSON.stringify(p)});state.keys.editId='';toast('Key 已更新','success');await renderKeys()}catch(error){toast(error.message,'error')}}}document.querySelectorAll('[data-act]').forEach(b=>b.onclick=async()=>{const k=keys.find(v=>v.id===b.dataset.id);if(!k)return;const a=b.dataset.act;if(a==='detail'){location.hash=`#/key-detail?id=${encodeURIComponent(k.id)}`;return}if(a==='edit'){state.keys.editId=k.id;await renderKeys();return}if(a==='regen'){if(!confirm(`确认重新生成 Key「${k.name}」？旧 Key 将立即失效。`))return;try{const d=await api(`/keys/${k.id}/regenerate`,{method:'POST'});state.keys.revealedKey=d.key||'';toast('Key 已重新生成','success');await renderKeys()}catch(error){toast(error.message,'error')}}if(a==='delete'){if(!confirm(`确认删除 Key「${k.name}」？`))return;try{await api(`/keys/${k.id}`,{method:'DELETE'});state.keys.editId='';toast('Key 已删除','success');await renderKeys()}catch(error){toast(error.message,'error')}}})}
