import {title,shell,head,api,table,esc,formatDate,formatMs,codeBadge,badge,state} from './common.js'
export async function renderDashboard(){title('仪表盘');shell('dashboard',head('仪表盘','系统整体运行状态概览')+'<div class="card"><span class="loader"></span></div>');const [stats,logsRes]=await Promise.all([api('/stats'),api('/logs?limit=20')]);const rows=(logsRes.logs||[]).map(v=>`<tr><td>${esc(formatDate(v.created_at))}</td><td>${esc(v.account_name||'-')}</td><td>${esc(v.api_key_name||'-')}</td><td><span class="mono">${esc(v.method||'-')}</span></td><td>${esc(v.model||'-')}</td><td><span class="mono small">${esc(v.path||'-')}</span></td><td>${codeBadge(v.status_code)}</td><td>${esc(formatMs(v.duration_ms))}</td></tr>`);shell('dashboard',`${head('仪表盘','系统整体运行状态概览','<button id="dashboard-refresh" class="btn">刷新</button>')}<section class="grid-4"><div class="card stat-card"><h3>📦 账号总数</h3><div class="stat-value">${esc(stats.total_accounts??'-')}</div><div class="muted small">当前托管的 copilot-api 账号数</div></div><div class="card stat-card"><h3>🔑 启用的 Key</h3><div class="stat-value">${esc(stats.enabled_keys??'-')}</div><div class="muted small">可直接用于代理调用的 Key</div></div><div class="card stat-card"><h3>📈 今日请求</h3><div class="stat-value">${esc(stats.today_requests??'-')}</div><div class="muted small">按日志统计的今日代理请求</div></div><div class="card stat-card"><h3>🧾 历史总请求</h3><div class="stat-value">${esc(stats.total_requests??'-')}</div><div class="muted small">累计记录到日志的请求总数</div></div></section><section class="grid-2"><div class="card"><div class="card-title"><h2>系统状态</h2></div><div class="kv-list"><div class="kv-row"><div class="key">登录用户</div><div>${esc(state.user?.username||'-')}</div></div><div class="kv-row"><div class="key">角色</div><div>${badge(state.user?.role||'user')}</div></div><div class="kv-row"><div class="key">静态前端</div><div><span class="badge info">原生 HTML</span></div></div><div class="kv-row"><div class="key">数据库驱动</div><div><span class="badge success">纯 Go SQLite</span></div></div></div></div><div class="card"><div class="card-title"><h2>轻量化说明</h2></div><div class="alert info">当前版本已移除前端构建链，并使用无 CGO 的 SQLite 驱动，更适合小内存服务器和 Docker 构建环境。</div></div></section><section class="card"><div class="card-title"><h2>最近请求</h2></div>${table(['时间','账号','Key','方法','模型','路径','状态','耗时'],rows,'暂无请求日志')}</section>`);document.getElementById('dashboard-refresh').onclick=()=>renderDashboard()}
